---
title: Our Full Research Team Directory
layout: 'base.njk'
---

<article class="main-page-content">
    <div class="container flow">
        <h1 class="section-title text-center">{{ title }}</h1>
        <p class="text-center">Meet the dedicated researchers and staff driving our projects forward.</p>

        <ul role="list" class="team-list team-list--full">
            {# Loop through the ENTIRE team collection #}
            {%- for member in collections.team | sort("data.sortOrder") -%}
                {% include 'team-snippet.njk' %}
            {%- endfor %}
        </ul>

    </div>
</article>